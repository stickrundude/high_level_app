specVersion: "v1beta"
serviceId: "high_level_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "high_level_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
